{
  "name": "kaileemcgee.com",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "watch --spawn=false 'node app/main.js'",
    "start": "node app/main.js"
  },
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.3"
  },
  "devDependencies": {
    "watch": "^0.16.0"
  }
}
